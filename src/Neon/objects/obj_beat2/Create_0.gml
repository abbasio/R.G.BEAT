//audio_play_sound(snd_neon2, 10, true);


width = display_get_gui_width(); //Establishes width of screen

rhythm = 30; //Dictates speed at which bar reaches center. Higher value = slower beat

buffer = 240; //Gives more room for player error on beat

lbar_x = 0; //Sets origin of left bar at the far left of screen	

rbar_x = 1920; //Sets origin of right bar at the far right of screen